services:
  angular:
    container_name: Angular
    build:
      context: ..
      dockerfile: setup/Dockerfile.prod
      target: production
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/etc/letsencrypt/live/frontend-infernum-original.duckdns.org-0001/fullchain.pem:/etc/letsencrypt/live/frontend-infernum-original.duckdns.org/fullchain.pem"
      - "/etc/letsencrypt/live/frontend-infernum-original.duckdns.org-0001/privkey.pem:/etc/letsencrypt/live/frontend-infernum-original.duckdns.org/privkey.pem"
    restart: unless-stopped
