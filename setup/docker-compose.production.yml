services:
  angular:
    container_name: Angular
    build:
      context: ..
      dockerfile: setup/Dockerfile.prod
      target: production
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/etc/letsencrypt/live/frontend-infernum-original.duckdns.org/fullchain.pem:/etc/letsencrypt/live/frontend-infernum-original.duckdns.org/fullchain.pem:ro"
      - "/etc/letsencrypt/live/frontend-infernum-original.duckdns.org/privkey.pem:/etc/letsencrypt/live/frontend-infernum-original.duckdns.org/privkey.pem:ro"
    restart: unless-stopped