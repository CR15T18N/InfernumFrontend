services:
  angular:
    container_name: Angular
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ".:/app"
      - "/app/node_modules"
    ports:
      - "4200:4200"
    environment:
      - NODE_ENV=development
